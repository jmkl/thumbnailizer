export type rawfilter_data = {
    texture: number;
    clarity: number;
    sharpen: number;
    noise_reduction: number;
    colornoise_reduction: number;
    vibrance: number;
    saturation: number;
    temp: number;
    tint: number;
  };

  export type oilpaint_data = { 

    stylization:number,
    cleanliness:number,
    brushScale: number,
    microBrush: number,
  };

